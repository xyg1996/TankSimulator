import os

class Path:

    def __init__(self):
        self.app_home_dir = os.path.normpath(os.path.dirname(os.path.realpath(__file__)))
        self.gui_dir = os.path.join(self.app_home_dir,"gui")
        self.template_dir = os.path.join(self.app_home_dir,"template")

p = Path()